from app import app
import MySQLdb

db = MySQLdb.connect(host="ec2-54-158-19-194.compute-1.amazonaws.com", user="venmo", passwd="pass", db="VenmoDB")
cursor = db.cursor()

@app.route('/')
@app.route('/index')
def index():
	return "Hello, World!"


@app.route('/api/<user>')

def get_tran_data(user)
	request = "SELECT * FROM Transactions_NT WHERE ID1=%s"
	response = cursor.execute(request,user@app.route('/api/<email>/<date>')
def get_email(email, date):)
	responce = cursor.fetchall()
	db.close()
	response_list = []
        for val in response:
        	response_list.append(val)
	return response_list
